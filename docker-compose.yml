services:
  postgres:
    image: postgres
    container_name: postgres-db

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: hospital_db

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/liib/postgresql/data

volumes:
  postgres_data: